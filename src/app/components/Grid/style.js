import styled from 'styled-components';

// Card
export const Wrapper = styled.section`
  max-width: 72rem;
  width: 100%;
  margin-left: auto;
  margin-right: auto;

  &:after {
    clear: both;
  }
`